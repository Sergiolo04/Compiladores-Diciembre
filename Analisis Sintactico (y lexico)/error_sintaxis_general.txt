prueba() {
    var int a, b;

    a = (5 + 3;      // Falta cerrar paréntesis

    if (a > 0        // Falta cierres
        print("Hola\n")
    else
        print("Error\n");

    b = 3 * ;        // Operador colgante

    while (a         // Falta paréntesis
        a = a - 1;
}
